---
title: '{{title}}'
sidebar_label: '{{sidebar_label}}'
id: {{id}}
description: '{{description}}'
tags:
{{#each tags}}
- {{this}}
{{/each}}
toc_min_heading_level: 2
toc_max_heading_level: 4
---

<!-- Style rules (do not include in output):
- Title: Title Case, action-based (not feature-name-based). Body does NOT repeat the title as # H1.
- H2/H3 headings: Sentence case, action-based.
- UI field names: **bold**. Values and examples: `code case`.
- Active voice, simple present tense. Imperative voice in steps.
- Progressive disclosure: basic use first, then advanced, caveats, edge cases.
- Callouts: :::note for extra details, :::warning for restrictions. Use sparingly.
- Code blocks: add showLineNumbers and highlight specific lines (e.g. ```yaml showLineNumbers {3-5}```).
- Banned words: leverage, utilize, harness, streamline, robust, seamless, empower, facilitate, delve into, cutting-edge.
- Early Access feature: needs the badge component + callout (see style-guide.md's
  "Early Access features" section) -- badge in the sidebar entry if this whole page is
  new, or next to the heading if adding a new EA section to an existing page.
- Section guide:
  - ## Overview     [required]
  - ## Configuration [required if the feature has YAML/CRD fields; omit for pure UI-only pages]
  - ## Examples     [required when typical usage can be shown; omit for pure-reference pages]
  - ## Reference    [required for pages with config fields; omit for workflow/how-to pages]
  - Add extra H2 sections (e.g. ## Prerequisites, ## Next steps) as appropriate.
-->

## Overview

{{overview}}

## Configuration

{{configuration}}

## Examples

{{examples}}

## Reference

{{reference}}
