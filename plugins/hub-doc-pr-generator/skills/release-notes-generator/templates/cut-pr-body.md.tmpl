## Summary

Assembles the `## Gateway {{version}}` release-notes.mdx section from
{{fragment_count}} fragment(s) written across {{source_pr_count}} doc PR(s),
and deletes those fragments now that they're consumed.

- Fragments assembled: {{fragment_filenames}}
- Source doc PRs: {{source_prs}}

## Compatibility matrix

Auto-filled from `go.mod` at {{version}}, overlaid with any component-version
deltas the individual fragments carried in their own front matter. Any row
still TBD needs a human to fill in before merging:

{{tbd_checklist}}

## Reviewer checklist

- [ ] Section order matches convention: Graduated to GA first, feature subsections newest-first, compatibility matrix last
- [ ] No fragment content was dropped or altered beyond front-matter stripping
- [ ] Compatibility matrix TBDs above are resolved (or intentionally left for hub-doc team follow-up)
- [ ] Deleted fragment files match exactly the ones assembled into this section (see list above)

---

_Generated by `release-notes-generator cut`. This is the only PR that touches `release-notes.mdx` directly for this release._
